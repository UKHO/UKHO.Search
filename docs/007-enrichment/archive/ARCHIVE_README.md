# Archive Policy

This folder contains superseded specification versions for Work Package `docs/007-enrichment/`.

## Rules
- Archived files are immutable snapshots.
- Do not edit files in `archive/`.
- When a spec is updated, create a new version in the Work Package root and move the previous version into `archive/`.
