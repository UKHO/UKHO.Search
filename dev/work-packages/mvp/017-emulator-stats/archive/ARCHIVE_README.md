# Archive policy

This folder contains superseded specification versions for work package `dev/work-packages/mvp/017-emulator-stats/`.

Rules:
- Archived specs are immutable snapshots and MUST NOT be edited.
- The latest (current) spec versions live in the work package root.
- When a spec is updated, a new version is created and the prior version is moved here unchanged.
